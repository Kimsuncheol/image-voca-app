/**
 * =============================================================================
 * ADMIN CODES COMPONENTS INDEX
 * =============================================================================
 * Central export file for all admin codes management components
 *
 * COMPONENT ORGANIZATION:
 * 1. GenerationForm - Code generation form for creating admin registration codes
 * 2. ActiveCodesList - List of active/expired codes with management actions
 *
 * USAGE:
 * Import components from this file in the main admin-codes.tsx:
 * import { GenerationForm, ActiveCodesList } from './admin-codes/components';
 * =============================================================================
 */

export { GenerationForm } from './GenerationForm';
export { ActiveCodesList } from './ActiveCodesList';
