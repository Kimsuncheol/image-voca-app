/**
 * =============================================================================
 * PROMOTION CODES COMPONENTS INDEX
 * =============================================================================
 * Central export file for all promotion codes admin components
 *
 * COMPONENT ORGANIZATION:
 * 1. GenerationForm - Code generation form with all configuration options
 * 2. ActiveCodesList - List of active/inactive codes with management
 *
 * USAGE:
 * Import components from this file in the main promotion-codes.tsx:
 * import { GenerationForm, ActiveCodesList } from './components';
 * =============================================================================
 */

export { GenerationForm } from './GenerationForm';
export { ActiveCodesList } from './ActiveCodesList';
